Put the Snap Analytics logo here as one of:

- `snap_logo.svg` (best, stays sharp at any size)
- `snap_logo.png` (use a transparent background, at least 600 px wide)

Optionally add `snap_icon.png` (the square mark on its own). It is shown when
the sidebar is collapsed and as the browser tab icon.
